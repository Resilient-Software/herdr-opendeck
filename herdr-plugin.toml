id = "com.thomasrooney.herdr-opendeck"
name = "Herdr OpenDeck"
version = "0.1.0"
min_herdr_version = "0.8.0"
description = "Mirror the Herdr session onto a Stream Deck via OpenDeck."
platforms = ["macos", "linux"]

[[build]]
command = ["sh", "scripts/install.sh"]
platforms = ["macos", "linux"]
